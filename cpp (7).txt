



cpp-в общем.
стек - область в памяти,где выполняется программа,туда помещаются задачи,и
по очереди их выполняет(программа)
реализована через "последний зашел первый вышел"

cpp-string

есть функция getline(std::cin, str) которая считывает всю строку(str - переменная с типом строки).

переменая.size() - размер строки

переменная[элемент строки]

переменная[элемент строки] = 'L' можно заменять или добавлять элементы.

строки можно складывать(конкотенация): переменная + " " + переменная



cpp-в общем.
использование using namespace std - является дурным тоном.

cpp-ветвление
elif не существует в с++ 
есть else if (выражение)
{

}

&& - логическое и
! - логическое не
|| - логическое или


cpp-циклы for
можно например написать for(;;) что будет эквивалетно while(true) то есть
оно будет воспроизводится до тех пор пока не встретит в теле цикла 
инструкцию break.

продолжу

например заместо
for(int i = 0; i < 10;i++)
{

	std::cout << i;

}

можно сделать так
int i;

for(; i < 10;)
{

	std::cout << i;
	i++

}

массивы c++

создание 

тип имя[ограничение\константа]

num_whats{};

array[num_whats++] = data \ data - данные,будто строки,или еще что-то,числа!

array[0] = data_new \ обращение к нулевому для компьютера и для 1 элемента массива для нас.

желательно также создать переменную которая будет отслеживать реальные данные в массиве,т.к она может увеличиваться каждый раз при появление новых данных

много мерные массивы array[1][0] - двух мерный массив.


for(int i = 0; i < size; i++)
{
	arr[i] = i;
	std::cout << arr[i] << std::endl;
}

причесление i к элементам

c++ sizeof()

возвращает число в байтах данных,массивов,переменных

sizeof(arr)/sizeof(int); // вывод элементов массива,
 
sizeof(arr)/sizeof(arr[0]);


c++ vector
динамический массив - по сути
std::vector<тип вектора> название

Безопасные преобразования
bool в char
bool в int
bool в double
char в int
char в double
int в double

небезопасные преобразования
double в int
double в char
double в bool
int в char
int в bool
char в bool

Константы
constexpr - выражение

Скобки 
скобки всему голова в выражения и операциях

цели программиста для программы
написать простую,модифицируемую,простую,эффективную программу

инструкция throw - генерирует исключение ошибок показывая что произошло что-то неправильное

ошибки диапазона вектора,когда мы пытаемся обратиться к элементу за вектором.

try catch тоже своего рода конструкция генератора исключение ошибок ;-)

с++ - страуструп(720) 

тернальный оператор -- дошел

"Пиздеть - ещё не думать, но всё ещё не мешки ворочать" цитаты великих)))

1. Декомпозиция - выявление самых мелких фундаментальных объектов изучения.
2. Написать определение нового термина. Привести пример.
3. "ЗАЧЕМ?" Написать причины, по которым предмет изучения нужен. Тезисно, коротко, своими словами.
4. Моделирование 2-3 конкретных ситуаций, при которых пригодилось бы данное знание.
5. Структуризация - объединение похожих элементов и выяснение, в чем между ними разница. Подкрепить более конкретными примерами, которые отражали бы эту разницу.
6. Иерархизация - выстраивание связей между простыми частями и более сложными.

сссылки на что либо в параметрах функции 8 глава страуструп
ссылки & константные ссылки const левое значение& что-то

классы страуструп
перегрузка оператора - (добавление типу(классу) операторы встроенны в с++) operator какой-то оператор типу инкримента(может быть оформлен ввиде функции с ссылками)


методы гетовый и сетовый - сет установить значение а гет получить его

9.1 зачем нужны типы определенные пользователем(класс) - что бы выразить идею объекта,скажем составить некий шаблон объекта и его свойств характерные ему.
9.2 компоненты класса называются его членами.
    бывают член-данные и функция-член
    к их доступу можно обратиться путем того что написать объект.член
    а можно также: тип имякласса::функция-член
9.3 итак,принцип инкапсуляции гласит(один из принципов ооп) что реализацию нужно держать закрытой,в том плане,не давать к ней никого доступа извне,для таких случаев существует
    метка private: (в классе),а за интерфейс обычно отвечает метка public: (также в классе)
9.4 в одном из подразделов,раздела, я узнал что такое есть - конструктор класса,это функция-член имя которой совпадает с именем класса,и используется для инициализации объекта
(НЕ ИМЕЕТ НИКАКОГО ТИПА, ИМЕЕТ ТОЛЬКО ИМЯ КАК У КЛАССА В КОТОРОМ РЕАЛИЗУЕТСЯ(КАК ФУНКЦИЯ) - public секция)
struct Date 
{
    int у, m, d;
    Date(int у, int m, int d); // Проверяем корректность даты
                               // и выполняем инициализацию

};
   также в одном из подразделов данного раздела я еще узнал как можно инциализировать данные-члены 
	Date::Date(int yy,int mm,int dd)
	    :y{yy}, m{mm}, d{dd}
        {
		тут например проверка на то что дата корректна методов исключени и генератором ошибок через try catch 
	}
9.5 перечисление enum class что-то
    что бы обратиться к элементу перечисления мы должны написать что-то::элемент-перечисления
    если не инциализировать первый элемент то отсчет будет начинаться с 0.
    Month m = Month::feЬ;
9.6 
9.7 конструктор по умолчанию Date();
Date::Date()
	:y{2001},m{Month::jan},d{1}
const - в начале объявление функции,возвращает константный объект
const после списка параметров функции,указывает,что функция не изменяет объект,для которого она вызывается,таким образом,
функция становится доступной для вызова через константные объекты


std::cin.clear() - очищает поток ввода
std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n'); -помогает

перегрузка функций
название одно
и разные реализаци

перегрука конструктора класса
название конструктора,но различия в реализации и параметрах.

fstream - библиотека для работами с файлами,делая над ними некие операции.

установить кодировку файла 1 делом (setlocale(LC_ALL, "UTF16");)
создать объект ofstream для вывода в файл,данных. std::ofstream fout;
fout.open("test.txt"); - передаем в качестве аргумента название с расширением файла
// здесь операции
fout << s << '\n';
fout << a + b;
// закончили,закрыли файл
fout.close();
ifstream - поток для чтения из файла,ofstream - для записи в файл

состояние потока - good(),eof(),fail(),bad()
good - операция прошла успешно
eof - достигнут конец ввода(конец файла)
fail(неприятность,например ждали цифру,получили букву)
bad(ошибка серьезнее чем в состоянии fail,например ошибка чтение диска)
есть еще разные методы ввиде clear()
Функция unqet () представляет собой более короткий вариант функции putback() который основывается на 
предположении. что поток помнит, какой символ был последним, и поэтому его не обязательно указывать явно. 

настройка потоков ввода и вывода
начинается все с того,что мы должны предоставить удобные представление и читабельности информации - дополнение '\t' - табуляция
octol - восьмеричная система счисления(std::oct)
hex - шестнадцетиричная(std::hex)
decimal
все выше являются только для типа int,то есть целочисленных,для FLOAT это тема не работает
std::dec - перевод потока для вывода 10-чных цифр
std::showbase - показать основание счисление для числа 0x для 16-рички и 0 для 8-рички в начале(префиксы)
std::noshowbase - не показывать основание (префиксы)

    std::cin.unsetf(std::ios::dec);
    std::cin.unsetf(std::ios::oct);
    std::cin.unsetf(std::ios::hex);
флаги для установки префиксов при считывание данных!

вывод чисел с плавающей точкой
fixed
scientific
defaultfloat

также есть манипулятор setprecision(число)
если 
defaultfloat - точность представляет собой общее кол-во цифр
scientific - точность представляет собой кол-во цифр после десятичной точки
fixed - точность представляет собой кол-во цифр после десятичной точки

поля
#include <iomanip> - что бы были доступны
устанавливать поля - std::setw(число)

режимы открытия файлов для класса объекта fstream fs{"myfile.txt", ios_base::in|ios_base::out};
указание имение файла + двух его режимов
| - побитовое или,которое можно использовать,для объединения режимов

список режимов
ios_base::out - открыть файл для записи
ios_base::in - открыть файл для чтения
ios_base::binary - бинарный режим
ios_base::ate - в конец(открыть и перейти в конец файла)
ios_base::app - добавление(запись в конец файла)
ios_base::trunc - обрезать файл до нулевой длинны.

есть позиционирование чтение\ввода и для записи \ вывода для класса объекта fstream fs
начинается с нуля и так далее
seekg(число); - перенести позицию чтения (g означает "get") на число указаное в качестве аргумента в параметрах, ячеек вперед
seekp(число); - перенести позицию записи (p означает "put") на число указаное в качестве аргумента в параметрах, ячеек вперед
при чтении и записи,позиции данных функций увеличиваются.

потоки для строк
getline(std::cin,объект) - для считывание строки полностью с пробелами,т.к оператор ">>" -не умеет это делать

функции std::cin.get() считывает отдельный символ в свой аргумент пробельные символы при этом не игрнорируются/

через указатель this можно обращаться к членам класса '->'
его также можно разыменовывать
и также можно использовать для вызываемого объекта,такой пример
const Stock& Stock::topval(const Stock & s) const 
{ 
	if (s.total_val > total_val) 
		return s; 
	else 
		return *this; 
}
два объекта 
stock a
stock b
вызываем метод
a.topval(b) - если б больше то вернет б,но если а больше то вернет а
b.topval(a) - если а больше то вернет а,но если б то вернет б

Если метод нуждается в явном доступе к объекту, который его вызвал, он может сделать это через указатель this. Указатель this устанавливается в адрес вызывающего объекта, поэтому 
выражение *this является псевдонимом самого объекта.

добавление про ооп перегрузку и прочее
можно перегружать разные операторы в с++ но не выдумывать свои

class Time
{
private:
    int hours;
    int minutes;
public:
    Time();
    Time(int h, int m = 0);
    void AddMin(int m);
    void AddHr(int h);
    void Reset(int h = 0, int m = 0);
    Time operator+(const Time& t) const;
    Time operator-(const Time& t) const;
    Time operator*(const Time& t) const;
    friend Time operator*(double m, const Time& t); //друг классу
};

Time Time::operator-(const Time& t) const
{

    Time diff;
    int val1, val2;
    val1 = t.minutes + 60 * t.hours;
    val2 = minutes + 60 * hours;
    diff.minutes = (val2 - val1) % 60;
    diff.hours = (val2 - val1) / 60;
    return diff;
}
Time Time::operator+(const Time& t) const
{
    Time sum;
    sum.minutes = minutes + t.minutes;
    sum.hours = hours + t.hours + sum.minutes / 60;
    sum.minutes %= 60;
    return sum;
}

Time Time::operator*(const Time& t) const
{
    Time result;
    long totalminuts = hours * 60 + minutes;
    result.hours = totalminuts / 60;
    result.minutes = totalminuts % 60;
    return result;
}

 

это все примеры перегрузок через функции члены и прототипов объявленнных в классе

пример friend функции к классу на мой взгляд тут можно сказать одно,нету явного обращения к классу,но при этом эта функция есть в списке прототипов класса
Time operator*(double m,const Time& t)
{
    Time result;
    long totalminuts = t.hours * 60 + t.minutes;
    result.hours = totalminuts / 60;
    result.minutes = totalminuts % 60;
    return result;
}

перегрузка оператора потока вывода
std::ostream& operator<<(std::ostream& os,const c_name& obj)
{

	os<< ...; // отображение содержимого объекта
	return os;

}

#define _CRT_SECURE_NO_WARNINGS 1
для того что бы использовать старый\легаси код функциями

возврат значение(без ссылок)
нужен для буферной переменной в функциях
возврат ссылок если невозможно обратиться к коструктору копирования(std::ostream) и эффективнее в плане производительности,но ссылка должна указывать на объект(в случае чего,заход за область видимости,объект 'умирает')
если в параметрах функции объекты объявленны как КОНСТАНТНЫЕ ссылки то должна возвращаться константная ссылка
+ по возврате по ссылке не активируется конструктор копирования

указатели при приеме как параметра,хоть где,нужно проверять на нулевой указатель

операция new с размещением

тип указателя* p;
для второго объекта
p = new (buffer + sizeof(тип)) //buffer массив в куче,но конкретно тут размещается в буфере,куча->буфер->объект(то есть объект в буфере)
но нужно очищать сам buffer путем delete[] buffer; и явный вызов деструктора для указателя.
p->~деструктор();

почленное копирование для классов с членами других классов стивен прата 631 страница - оттуда начинай
Наследование ооп
унаследоваему классу нужны собственные конструкторы
Конструкторы должны предоставлять данные для новых членов, если они есть, а 
также для унаследованных членов
Производный класс не имеет непосредственного доступа к закрытым членам базового класса, и он вынужден обращаться к ним с помощью методов базового класса.
объект базового класса должен быть создан до 
того, как программа войдет в тело конструктора производного класса.
Ниже перечислены основные моменты, которые следует знать о конструкторах 
для производных классов. 
• Сначала создается объект базового класса. 
• Конструктор производного класса должен передавать информацию базового 
класса конструктору базового класса через список инициализаторов членов. 
• Конструктор производного класса должен инициализировать данные-члены, добавленные в производном классе. 
 То есть сначала выполняется тело деструктора производного класса, а затем автоматически вызывается деструктор базового класса. 
 Ссылкам и указателям производного класса запрещено присваивать объекты и адреса базового класса: 
объекту базового класса можно присвоить объект производного класса
применение virtual(ключевого слова) он определят какой метод использует,если метод выызывается не объектом а ссылкой или указателем,без virtual,программа выбирает на основе типе
ссылки или указателя,но наоборот же программа выбирает метод,основываясь на типе объекта,на который указывает ссылка или указатель
Поэтому общей рекомендацией будет объявление в базовом классе в 
качестве виртуальных тех методов, которые могут бьпъ переопределены в производном классе. Если метод объявлен в базовом классе как виртуальный, он автоматически
является виртуальным и в производном классе. Однако в объявлениях производного класса также рекомендуется указывать, какие функции являются виртуальными, с помощью ключевого слова virtual.
Четвертый момент заключается в том, что в базовом классе объявлен виртуальный
деструктор. Это необходимо для правильной последовательности вызовов деструкторов при уничтожении производного объекта. Данный вопрос мы обсудим более подробно ниже в данной главе
Код не обязан применять разрешение контекста 
для Balance () , поскольку этот метод не переопределялся в производном классе. 
Если деструкторы не виртуальные, то вызывается только деструктор, соответствующий типу указателя
Если метод базового класса будет переопределяться в производном классе, его необходимо объявить виртуальным. Если метод не будет переопределяться, он должен быть не виртуальным.

пример использования массива указателей Brass:
#include <iostream>
#include <string>
#include <vector>
#include <cmath>
#include <algorithm>
#include <fstream>
#include <iomanip>
#include <cstdlib>
#include <stdexcept>
#include <sstream>
#include <cstring>
#include <regex>
#include <list>
#include <forward_list>
#include <unordered_map>
#include <array>
#include <random>


class Brass
{
private:
    std::string fullName;
    long acctNum;
    double balance;
public:
    Brass(const std::string& s = "Nullbody", long an = -1,double bal = 0.0);
    void Deposit(double amt);
    virtual void Withdraw(double amt);
    double Balance() const;
    virtual void ViewAcct() const;
    virtual ~Brass() {}
};
//Brass Plus Account Class
class BrassPlus : public Brass
{
private:
    double maxLoan;
    double rate;
    double owesBank;
public:
    BrassPlus(const std::string& s = "Nullbody", long an = -1,double bal = 0.0, double ml = 500,double r = 0.11125);
    BrassPlus(const Brass& ba, double ml = 500,double r = 0.11125);
    virtual void ViewAcct()const;
    virtual void Withdraw(double amt);
    void ResetMax(double m) { maxLoan = m; }
    void ResetRate(double r) { rate = r; };
    void ResetOwes() { owesBank = 0; }
};

// для форматирования
typedef std::ios_base::fmtflags format;
typedef std::streamsize precis;
format setFormat();
void restore(format f, precis p);

// методы brass
Brass::Brass(const std::string& s, long an, double bal)
{
    fullName = s;
    acctNum = an;
    balance = bal;
}
void Brass::Deposit(double amt)
{
    if (amt < 0)
    {
        std::cout << "negative deposit not allowed" << '\n';
        std::cout << "deposit is canceled.";
    }
    else
    {
        balance += amt;
    }
}
void Brass::Withdraw(double amt)
{
    format initiaState = setFormat();
    precis prec = std::cout.precision(2);
    if (amt < 0)
    {
        std::cout << "withdrawal amount must be positive" << '\n';
        std::cout << "withdrawal canceled.";
    }
    else if(amt <= balance)
    {
        balance -= amt;
    }
    else
    {
        std::cout << "withdrawal amount of $" << amt
            << "exceeds your balance.\n"
            << "withdrawal canceled.\n";
    }
    restore(initiaState, prec);
}
double Brass::Balance() const
{
    return balance;
}
void Brass::ViewAcct() const
{
    format initiaState = setFormat();
    precis prec = std::cout.precision(2);
    std::cout << "client: " << fullName << "\n";
    std::cout << "Account number: " << acctNum << '\n';
    std::cout << "balance $: " << balance << '\n';
    restore(initiaState, prec);
}

// методы brassplus
BrassPlus::BrassPlus(const std::string& s, long an, double bal, double ml, double r)
    :Brass(s,an,bal)
{
    maxLoan = ml;
    owesBank = 0.0;
    rate = r;
}
BrassPlus::BrassPlus(const Brass& ba, double ml, double r)
    :Brass(ba)
{
    maxLoan = ml;
    owesBank = 0.0;
    rate = r;
}
void BrassPlus::ViewAcct() const
{
    format initiaState = setFormat();
    precis prec = std::cout.precision(2);
    Brass::ViewAcct();
    std::cout << "Maximum loan: $" << maxLoan << '\n';
    std::cout << "Owed to bank: $" << owesBank << '\n';
    std::cout.precision(3);
    std::cout << "Loan Rate: " << 100 * rate << '%\n';
    restore(initiaState, prec);

}
void BrassPlus::Withdraw(double amt)
{
    format initiaState = setFormat();
    precis prec = std::cout.precision(2);
    double bal = Balance();
    if (amt <= bal)
    {
        Brass::Withdraw(amt);
    }
    else if (amt <= bal + maxLoan - owesBank)
    {
        double advance = amt - bal;
        owesBank += advance * (1.0 + rate);
        std::cout << "advance bank: $" << advance << '\n';
        std::cout << "finance charge: $" << advance*rate << '\n';
        Deposit(advance);
        Brass::Withdraw(amt);
    }
    else
    {
        std::cout << "credit limit exceeded.Transaction cancelled.\n";
    }
    restore(initiaState, prec);
}
format setFormat()
{
    return std::cout.setf(std::ios_base::fixed,std::ios_base::floatfield);
}
void restore(format f,precis p)
{
    std::cout.setf(f, std::ios_base::floatfield);
    std::cout.precision(p);
}

int main()
{
    const int CLIENTS = 4;
    Brass* p_clients[CLIENTS];
    std::string temp;
    long tempnum;
    double tempbal{0};
    char kind;
    for (int i = 0; i < CLIENTS; i++)
    {
        std::cout << "Enter client's name: ";
        std::getline(std::cin, temp);
        std::cout << "Enter client's account number: ";
        std::cin >> tempnum;
        std::cout << "Enter opening balance: $";
        std::cin >> tempbal;
        std::cout << "Enter 1 for Brass Account or "
            << "2 for BrassPlus Account: ";
        while (std::cin >> kind && (kind != '1' && kind !='2'))
        {
            std::cout << "enter either 1 or 2: ";
        }
        if (kind == '1')
        {
            p_clients[i] = new Brass(temp, tempnum, tempbal);
        }
        else
        {
            double tmax, trate;
            std::cout << "Enter the overdraft limit: $";
            std::cin >> tmax;
            std::cout << "Enter the interest rate "
                      << "as a decimal fraction: ";
            std::cin >> trate;
            p_clients[i] = new BrassPlus(temp, tempnum, tempbal, tmax, trate);
        }
        while (std::cin.get() != '\n')
            continue;
    }
    std::cout << '\n';
    for (int i = 0; i < CLIENTS; i++)
    {
        p_clients[i]->ViewAcct();
        std::cout << '\n';
    }
    for (int i = 0; i < CLIENTS; i++)
    {
        delete p_clients[i];
    }
    std::cout << "done.\n";
    return 0;
}

Полиморфное открытое наследование - тема для вспоминания виртуальных функций(их использование и наследования)

конструкторы не могут быть виртуальными.
Деструкторы должны быть виртуальными, за исключением тех классов, которые не используются в качестве базовых.
дружественные функции не могут быть виртуальными.
Концепция АБК(абстрактный базовый класс) объявляется чистая виртуальная функция,по типу virtual void area() = 0; // чистая виртуальная функция,значит что это абстрактный базовый класс
и нельзя создавать его объекты,только наследоваемые конкретные классы
АБК требует, чтобы его чистые виртуальные функции перегружались во всех 
конкретных производных классах - т.е. производный класс должен.подчиняться правилам интерфейса, установленным в АБК.
насчет АБК и protected - Члены производного класса имеют прямой доступ к защищенным членам базового класса, но не имеют прямого доступа к закрытым членам базового класса. То есть члены из защищенной категории ведут себя как закрытые члены для внешнего мира и как открытые члены для производных классов.

если базовый класс использует динамическое выделение памяти,но производный от него не использует,то деструктор,конструктор копирования,операцию присваивания,ничего из этого не надо,но если надо то берем другой способ.
если производный тоже использует динамическое выделение памяти то надо реализовывать каждый деструктор отдельно,конструктор копирования(ниже смотри на код),операция присваивания(ниже смотри код)

// конструкторы копирования
baseDМA::baseDМA(const baseDМA& rs) // конструктор копирования базового класса
{ 
	label = new char[std::strlen(rs.label) + 1]; 
	std::strcpy(label, rs.label); 
	rating = rs.rating; 
}
hasDМA::hasDМA(const hasDМA& hs) : baseDМA(hs) // конструктор копирования производного класса
{ 
style = new char[std::strlen(hs.style) + 1]; 
std::strcpy(style, hs.style);
}

// операции присваивания

baseDМA& baseDМA::operator=(const baseDМA& rs) // операция присваивания базового класса
{ 
if (this = &rs) 
	return *this; 
delete [] label; 
label = new char[std::strlen(rs.label) + 1]; 
std::strcpy(label, rs.label); 
rating = rs.rating; 
return *this;
}

hasDМA& hasDМA::operator=(const hasDМA& hs) 
{ 
if (this == &hs) 
	return *this; 
baseDМA::operator=(hs); 
delete [] style; 
style = new char[std::strlen(hs.style) + 1]; 
std::strcpy(style, hs.style); 
return *this;
}

полиморфизм в C++ опеределяется во время выполнение программы - виртуальные функции,либо в компилирумые уже известные функции(их перегрузка) и перегрузка операторов

explicit имя_конструктора() отключает неявное преобразование,в конструкторах с одним аргументом
Вспомните, что конструктор, который можно вызвать с одним аргументом, работает как функция неявного преобразования типа аргумента в тип класса. Часто такое поведение нежелательно
конструктор с одним аргументом - неявное преобразование,с двумя и более уже нет(но если предусмотреть в конструкторе,значение по умолчанию для второго параметра constructor(int b,double b =0); то это будет преобразование из int в тип класса)
функция преобразования - operator имяТипа();
должна быть методом класса,не должна иметь возвращаемый тип,и не должна иметь аргументов.
например для преобазования в тип double должна иметь такой тип operator double();
имя типа говорит в какой тип нужно преобразовывать

итак про повторение кода стивена праты отношение содержит
сначала показывает как использовать повторно код с помощью членов класса
потом с помощью закрытого наследование,условно говоря заместо объектов класса использовались имена классов и оператор размещения контекста для доступа к функциям!
+ в закрытом наследование когда хотят ввести в строку что-то то объект класса в дружественной функции ввода(оператора ввода) приобразуют в строку (std::string&) stu - stu объект класса Student
а когда нужно вернуть строку например имя для объекта то можно делать так (std::string&) *this для функции внутри класса!
сделал скрин с видами наследования и его свойствами
после посмотрел как использовать закрытые члены(функции и данные) например класс student : private std::valarray<double> в таком случае если используется закрытое наследование то открытые члены базового класса становятся закрытыми и либо нужно переопределять например
double Student::sum() const
{
	return std::valarray<double>::sum();
}
либо явно указать в производном классе что нужен этот метод в открытом доступе с помощью using в производном классе(в доступе public)
например 
class Student : private std::string,private std::valarray<double>
{
    ...
public:
	using std::valarray<double>::min;
	using std::valarray<double>::max;
};
можно делать и без using,но тогда будет не особо понятно,что вы имеете ввиду для программиста и компилятора!
виртуальный базовый класс(по отношению производному) нужен что бы при множеством наследовании не наследовать подобъекты например при случае есть абк класса worker и два производных от него класса singer(певец) и waiter(оффициант) потом от двух этих классов,множественное наследование(певца и официанта идет производный класс singerwaiter(поющий официант))
и вот без виртуальных базовых классов а в нашем случае без вирутальных классов singer и waiter будет наследоватся два подобъекта worker,нам такое не надо, мы берем и делаем class singer : virtual public worker и class waiter : virtual public worker и потом множественным наследованием class singerwaiter : public singer,public waiter, вот при таком случае не будет наследоваться два подобъекта worker а один
Если у класса есть непрямой виртуальный базовый класс,конструктор этого класса должен явно вызывать конструктор виртуального базового класса,за исключением тех случаев,когда достачно конструктора по умолчанию виртуального базового класса

при множественном наследование может создаваться неоднозначность ввиде вызывов методов для производного класса,от двух и более предков,например есть класс singingwaiter порожденный от двух классов singing и waiter у них обоих есть метод show(),но у производного класса его нет,тогда какой же использовать?
либо использовать операцию размещение контекста singingwaitr.singer::show(); либо переобределить метод внутри класса void singingwaiter::show() { Singer::show(); },но это характерно все для одиночного,можно вызывать сразу два метода базовых класса,но тогда он будет выводить имя сотрудника два раза а зачем нам это? Мы можем сделать методы -data() и выводить только части что ответственны внутри каждого класса сначала делаем в waiter и singer также и в worker потом делаем в нашем классе который порожден от двух предков(singingwaiter) также метод data туда суем вызовы методов предков кроме абк класса! и в методе singingwaiter::show() вызываем метод класса абк и метод из singingwaiter - data();

при наследование одного базового класса несколькими виртуальными и несколькими невиртуальными путями производный класс будет содержать один объект базового класса для представления всех виртуальных путей и отдельные объекты базового класса для каждого невиртуального пути

повторение кода - шаблоны - нужны что бы обеспечить более легкую работу с классами и со структурами данных, перед определением класса template<class Type>
нужны так же что бы не писать стеки,векторы под разные данные от числа с точками до целых

пример ниже
template<class Type>
class Stack
{
private:
    enum {MAX = 10};
    Type items[MAX];
    int top;
public:
    Stack();
    bool isempty();
    bool isfull();
    bool push(const Type& item);
    bool pop(Type& item);
};
template<class Type>
Stack<Type>::Stack()
{
    top = 0;
}

template<class Type>
bool Stack<Type>::pop(Type& item)
{
    if (top > 0)
    {
        item = items[--top];
        return true;
    }
    else
        return false;
}
и как пример как делать функции члены с шаблонами
разбирал как шаблоны взаимодействуют с указателями(с прошлым кодом выше,плохо)
пришлось делать Type указатель и делать из него массив в конструкторе и в целом так далее,в итоге пришлось делать деструктор,конструктор копирования,операцию присваивания

после вычитал что в шаблонах помимо типа можно указывать нетипизируемые аргуметы,например что бы создавать массив не в конструкторе класса,а прям брать аргумент из шаблона + шаблоны часто используются для контейнерных классов
template<class Type,int n>
после в классе можно сделать строчку 
Type ar[n]; // и не делать конструктор в принципе,но тогда теряется модифицируемость,но зато есть быстрота
после делали двухмерные массивы с помощью таких шаблонов

Еще одно новое свойство шаблонных классов - возможность указания значений по умолчанию для параметров типа: 
template <class Tl, class Т2 = int> 
В этом случае компилятор использует int в качестве типа Т2, если значение для Т2 отсутствует
также шаблоны могут использовать несколько параметров типов template<class T1,class T2>

есть неявное создание экземпляра это например ArrayTp<int,100> ab; // неявное
почему неявное?потому что компилятор не создает его,пока не используется его объект
явное же наоборот - с ключевым словом должно быть template class ArrayTP<int,100>;
  
также есть явная специализация,это когда например определенному типу или классу явно прописывает поведение(кодом,отдельно от остальной программы)
например есть класс 
template<class T>
class SortedArray
{
	// подробности неизвестны 
};
но нам известно что общий метод сортировок идет через оператор ">",для const char* строк такое не подойдет,но мы можем отдельно такому типу строк прописать поведение
template <> class SortedArray<const char*>
{

};
template <class T1,class T2> class Pair общая специализация
и есть частичная специализация template <class T1> class Pair<T1,int> специализация где T2- int
специализация template 	<> class Pair<int,int> явная специализация с инт и инт

также шаблоны могут быть вложенными и членами класса

также параметрами в шаблонах могут быть сами шаблоны!
template <template <typename T> class thing>
class Crab
{
private:
    thing<int> s1;
    thing<double> s2;
public:
    Crab() {};
    // предпологается что класс thing имеет push и pop методы
    bool push(int a, double x) { return s1.push(a) && s2.push(x); }
    bool pop(int& a, double& x) { return s1.pop(a) && s2.pop(x); }
};

насчет 'друзей' у шаблонных классов ими могут быть:
1 шешаблонные 'друзья' - просто функции,
2 связанные шаблонные друзья - функции с шаблонным параметром friend void reports(HasFriend<T>&) 
3 не связанные шаблонные друзья - template <typename C, typename D> friend void show2(C&, D&);

псевдонимы шаблоном (C++11)
например typedef std::array<double,12> arrd;
typedef std::array<int,12> arri;
typedef std::array<std::string,12> arrst;
и можно делать вот так arrd gallons;
arri days;
arrst months;
а можно и сделать вот так template<class T>
	using arrtype = std::array<T,12>; и можно делать arrtype<int> days; // заместо int может быть любой другой тип
и как оказывается using = typedef в C++11

друзья,исключение и многое другое(стивена прата)

как определить классу друга класс, friend class name_class;

упреждающие определение class TV; и реализацию сделать ниже(условный прототип)

как определить классу друга функции-член другого класс
friend type_func class_name::name_methods(//there parameters);

могут быть взаимные друзья, например class_name - друг для class_name2 и class_name2 друг для class_name

друзья могут быть общими(функции)

вложенные классы 

минусы
1. ослабление инкапсуляции(вложенный класс имеет доступ к членам класса,в котором он вложен)
2. вызывает путаницу в коде
3. невозможность использовать вложенный класс без указания того класса в котором он вложен
4. код становиться нечитабельным
плюсы
1. можно использовать возможности классов(конструкторы и тд) то бишь раньше в одной функции выполнялась например инициализация членов объекта то теперь,с прописанным конструктором их можно инициализировать через конструктор

исключения

механизм что бы отслеживать состоянии программы и проблемы

сначала была функции abort() для аварийного завершения программы
потом возврат по значению

теперь же блоки try и catch с механизмом исключений :-)
double hmean(double a, double b);

int main()
{
    double x, y, z;
    std::cout << "Enter two numbers: "; // запрос на введение 2 чисел
    while (std::cin >> x >> y)
    {
        try // начало блока try
        {
            z = hmean(x, y);
        } // конец блока try
        catch (const char* s) // начало обработчика исключений 
        {
            std::cout << s << '\n';
            std::cout << "Enter a new pair of numbers: "; // запрос на ввод новой пары чисел
            continue;
        } //  конец обработчика исключений
        std::cout << "Harmonic mean of " << x << " and " << y << " is " << z << '\n';
        std::cout << "Enter next set of numbers <q to quit>: ";
    }
    std::cout << "Bye!\n";
    return 0;
}

double hmean(double a, double b)
{

    if (a == -b)
    {
        throw "Bad hmean() arguments: a = -b not allowed";
    }
    return 2.0 * a * b / (a + b);

}

int main()
{
	std::string path = "myFile.txt";

    std::ifstream fin;
	fin.exceptions(std::ifstream::badbit | std::ifstream::failbit);

	try // пробуем выполнять,если все ок и ничего не генерирует исключения,прога выполняется нормально
		// но если генерируется исключение или бросило тогда отработает catch
	{
		std::cout << "Trying open file!\n";

		fin.open(path);
		// если все отработано успешно то файл открыт и выведется сообщение об его открытии
		std::cout << "file is open!\n";
	}
	catch (const std::ifstream::failure& ex) // что мы будем ловить в скобках
	{
		std::cout << ex.what() << '\n';
		std::cout << ex.code() << '\n';
		std::cout << "Error open file!\n";
	}

    return 0;
}

также в качестве исключение можно использовать объект класса исключения

если есть некая иерархия с классами исключений то исключения базового класса должно перехватываться последним в блоке catch а а исключения последнего производного класса первым в блоке catch так же можно перехватывать другие исключения через catch(...)

Познакомились с частью стандартной библиотеки C++ execption и stdexecption(почти так да) в них есть domain_error(если аргумент окажется вне области определения),invalid_error(функции были переданы непредвиденные значения),length_error(исключение связанное с длинной,например когда строка больше дозволенного),out_of_bounds(обозначение ошибок индексации) - все семейство logic_error

теперь семейство runtime_error - range_error(результат находится вне допустимого диапозона без потери значемости),underflow_error(возникает при вычислениях с плавающей точкой),overflow_error(переполнение для целых типов или типов с плавающей точкой)

итог logic_error - отражают ошибки которые можно исправить в коде, а ошибки runtime_error - которые избежать нельзя

также сегодня ловил исключение bad_alloc от оператора new при выделение больших объемов памяти!

итак сегодня(28.08.25) делал свои классы исключений с наследованием и реализовал их от logic_error

еще разбирал тему потери исключений и как менять поведение программы на непредвиденные и неперехваченные исключения либо с помощью функции set_terminate(//тут имя функции или адрес) и terminate() nonexcept; это про неперехваченные 

теперь с непредвиденные double Argh(double,double) throw(out_of_bounts); // тут используется спецификация исключений и какие исключения перехватывать

void myUnxcepete()
{
	throw std::bad_exception();
}

и с ними была создана функция unexpected() noexcept;

но ее поведение также можно поменять путем set_unexpected(// имя функции);

но также тогда надо будет в исходной функции Argh // ее определенеи внести в спецификацию bad_exception  

double Argh(double,double) throw(out_of_bounts,bad_exception);

исключения и их обработка сложны и должны быть в программе а не присоединены извне,но исключения увеличивают размер программы и скорость ее выполнения,но ущерб без них все ровно может быть большим чем без них,также добавлю,что управления исключениями в библиотеках не проще изучение самого языка,понимание тонких мест библиотек и классов столько же важно сколько и языка,но освоив обработку ошибок и исключений на основе документации и коде библы принесет большеую пользу программе и вам

RTTI

работает только для классов у которых есть виртуальные функции,также определение самого RTTI - стандартный механизм для определение типа объекта во время выполнение программы

dynamic_cast<Type*>(pt) - это выражение преобразует указатель pt в указатель типа Type*,если объект pt - имеет тип Type или унаследованный от него тип,в другом случае оператор вернет 0 то есть нулевой указатель

typeinfo - часть стандартной библиотеки C++ - также используется для RTTI например что бы сравнивать вот так typeid(name_class) == typeid(*pg) // pg разыменованый указатель тут условно говоря можно представить что мы проверяем лежит ли в указателе pg,объект типа class_name

также можно узнать с помощью typeid(*pg).name(); метод name - Возвращает строку которая показывает на какой объект(класс) указывает указатель(разыменнованный)

if(ps = dymamic_cast<Superb* >(pg))
	ps->Say(); // этот оператор сработает для всех классов поражденных от Superb

если используете typeid в длинной последовательности if else,возможно лучше будет использовать виртуальные функции и оператор dynamic_cast
 
приведение типа 

dynamic_cast<имя-типа> (выражение) - нужно что бы разрешить восходящие приведение типа внутри иерархии класса 

const_cast<имя-типа> (выражение) - нужен что бы удалить метку const 

static_cast<имя-типа>(выражение) - допустимо если имя-типа может быть неявно преобразовано в тип которое имеет выражение или наооброт,в любом другом случае это ошибка

пример : пускай High базовый класс для Low, а Pond не связанный класс тогда приведение High к Low и обратно допустимо,но вот Low к Pond нет

reintepritetr_cast<имя-типа>(выражение) - нужен для очень низкого уровня программирования

передача параметра по ссылке или по указателю мы передаем адрес в памяти и не копируем все в памяти(секрет от симпл код)|далее продолжу что конструктор копирования должен принимать в параметр const типо class_a(const class_a& obj) // это конструктор копирования в C++

итак началась новая глава стивена праты,начинается она о строках и их конструкторах и операциях взаимодействия с ними(и то что си строки это не тоже самое,что и класс строки в C++)

сначала был показан новый для меня конструктор ctor_str(20,'$') создает строку из 20 символов долларов

потом была такая строка char four[] = "something" // тоже строка только ее размер определит компилятор

также операция str_one += " oops!" // добавляет к текущей строке еще стоку,допустим была строка str_one("booms ")

станет применение операции += " oops!" -> str_one("booms  oops!")

также складывают строку four = two+three

также у строк можно менять отдельно элементы 

str_theys = "bib";

str_theys[0] = 'p' и будет строка pib

также по вводу строк

getline(str_obj,':') // будет выполнятся ввод до символа ':',если мы применяли си строки(массивы) то надо было бы указать число еще,то есть сколько может вместить массив

fin.open("Tobuy.txt") // иногда нужно сделать другой путь до файла "C:\\CPP\\Progs\\Tobuy.txt" или же "C:\CPP\Progs\Tobuy.txt",fin это файловый поток(считать из файла)

    std::ifstream fin;
    fin.open("Tobuy.txt");
    if (fin.is_open() == false)
    {
        std::cerr << "Cant open file.Bye.\n";
        exit(EXIT_FAILURE);
    }
    std::string item;
    int count = 0;
    getline(fin, item, ':'); // первые сардины
    while (fin) // пока идет считывание до конца файла
    {
        std::cout << item << '\t'; // вывод и табуляция
        getline(fin, item, ':'); // снова считывания до ':' и переинициализация строки item и опять вывод,и так до конца считывания(или до конца файла)
    }
    std::cout << "Done\n";
    fin.close();

считывает строки из файла(tobuy) считало первую строку до ':' потом еще и так далее до завершения считывания(До конца файла), то есть мы вчитывали в строку по кускам весь файл,а не весь файл сразу

1.5 дня работал со строками + методами и разными операциями

Строки можно сравнивать, по аски последовательностям, например числа будут меньше чем строки

< // эта операция 
== // эта операции
! = // эта операция

Также строки имеют length и size методы для сравнение общего количества символом в строке

также строки имеют метод find(const char* s,size_type pos = 0) и другие его части вроде find_first_of()

метод find() находит в вызываемой строке,первое вхождение подстроки s, начиная с pos,возвращает индекс найденной подстроки или std::string::npos если он не находит

методы строк capacity() возращает размер текущего блока памяти строки,а метод reserve(50),может запросить доп память для строки,также есть метод c_str(),обычную строку преобразовывает в си строку // заместо 50 может быть любое другое число

я добрался до умных указателей,и мне есть что сказать.

иногда они нужны по причине того,что простые указатели на char* или подобные,не очищаются по мере использования как объекты классов с помощью деструктора,а вот умные указатели так делают ;-)

умных указателя всего три auto_ptr,shared_ptr,unique_ptr

с ними можно делать почти все то же самое что и с обычными указателями,разыменовывания их,обращения к методам структур и классов.
Но также им нельзя присваивать адрес существующей какой-то области,например есть объект строки,и умному указателю на строку дают адрес объекта строки,так делать нельзя,т.к применяется delete к памяти не из кучи,что не допустимо

также с умными указателями не допустимо неявное преобразования.

по поводу умных указателей,какой из них нужно использовать?(про auto_ptr мы забываем это пережиток прошлого)

итак unique_ptr - если хотите избегать подобных ситуаций:


unique_ptr<string> рЗ (new string ( "auto") ; 
unique_ptr<string> р4; 
р4 = рЗ; // эта строка не выполнится с unique_ptr

но это выполнится если p4 будет инициализирован(присвоен через функцию,возращающая unique_ptr,поскольку он уничтожится,временный объект в функции,то ps4 будет единственным кто им владеет) также для unique_ptr имеется функции ps1 = std::move(ps2// тут имя объекта указателя unique_ptr) // это безопасное присваивание одного объекта другому

кароче если хотите один указатель с концепцией владения,и что бы у него не работала особо операция присваивания то unique_ptr,концепция владения - когда один умный указатель владеет один объектом,а операция присваивания это передача владения,также unique_ptr имеет вариант new[] и delete[] то есть с массивами,до C++ 17,shared_ptr тоже не работал с ними,но потом вроде все окей стало

ну можно выбрать еще один умный указатель который будет отслеживать сколько умных указателей ссылается на определенный объект объект,эта стратегия это подсчет ссылок.Пример - присваивание могло бы увеличивать подсчет ссылок на единицу,а удаление указателя уменьшать его.Тогда операция delete вызвалась бы только при удалении последнего указателя,это уже shared_ptr 

пример:

    std::shared_ptr<std::string> pwin;
    pwin = films[2]; // сначала удалится объект pwin,и счетчик ссылок будет = 1,и после объект films[2] который тоже shared_ptr и будет счетчик равен = 0,и следовательно произойдет удаление памяти

вообщем если нужно несколько указателей на один объект то shared_ptr если один с передачей права + работа с массивами до 17 стандарта, то unique_ptr(т.к он безопаснее чем auto_ptr)

теперь мы перешли к stl по курсу книги 

первое что мы рассматривали это вектор, и что с ним можно делать

например из интересного это Итератор,указатель или объект класса,у которого определенны операции как у указателя,они предоставляют однотипный интерфейс для классов контейнеров,включая те для которых обычные указатели не работают

вот так определяется итератор для вектора std::vector<тут тип>::iterator name_object;

name_object = vector_name.begin(); // name_object указывает на первый элемент вектора
*name_object = 22.4; // name_object теперь присваивает значение первому элементу
++name_object; //  name_object указывает на следующий элемент

циклы с итераторами for(it = vector_name.begin();it != vector_name.end() it++)
			std::cout << it << '\n';

есть еще разные методами вектора с итераторами например erase() - удаляет данный диапазон элементов вектора,пример:

vector_name.erase(vector_name.begin(),vector_name.begin()+2);// удаляет первый и второй элемент вектора диапозон итераторов работает так [it1,it2) он начинается с it1 и заканчивается it2,но сам it2 туда не входит

есть еще метод insert() позволяющий добавлять в вектор элементы,принимает три аргумента,первый позиция,после которой будут добавляться элементы,второй и третий итератор описывают добавляемый диапазон,пример:

vector_one;
vector_two;

vector_two.insert(vector_two.begin(),vector_one.begin()+1,vector_one.end()); // этот код добавляет элементы vector_one в vector_two за исключение первого элемента vector_one,и добавляет он их после первого элемента.

но лучше добавлять после конца,или с конца vector_two.end(),т.к на это потребуется меньше ресурсов пк

Итак сегодня 1 октября возобнил режим наконец,узнал про функцию sort,и как она сортирует она сортирует по знаку '<',если не указывать в аргументах функтор функции булевой,это 3 аргумент вот пример:

sort(vector_name.begin(),vector_name.end()); // использует оператор '<' для того типа,для которого она предопределена например если вектор int,для int,если пользовательского класса то '<' пользователя

и есть sort(vector_name.begin(),vector_name.end(),fuction_on_bool); // функция использует '<' для сравнения,но тут можно по своему определять что и как сортировать,она должна принимать два параметра типа,которые будем сравнивать,она - fuction_on_bool

также узнал про функции for_each(vector_name.begin(),vector_name.end(),some_function) // использует функцию some_function для каждого элемента в диапазоне от начала до конца,например может быть вывод,должна принимать параметр тип,причем тип должен быть тот же что и у контейнера.

и функция random_shuffle(books.begin(), books.end()) // в случайном порядке сортирует элементы 

также немного нового про цикл for диапазона :
double prices[5] = {53,35,6,3,2};
for(double x : prices)
	std::cout << x << '\n';
он может применятся альтернативой for_each() // for(auto x : books) // x будет Review
+ цикл for диапазона может менять содержимое контейнера
например можем сделать такую функцию :

void InflateReview(Review& r) {r.rating++;} // при этом важно параметр что бы был ссылка

и эту функцию можно было бы применить для каждого элемента for(auto& x : books) InflateReview(x);

переходя к обобщенному программированию,шаблонам,итераторам - это все способ не зависить от данных или структур данных,например реализация алгоритма find для какой угодно структуры данных,и для какого угодно типов данных

итераторы же обеспечивают доступ к контейнерам(структурам данных) не смотря на их тип и тип хранимый данных ими,то есть обобщенное перемещение по элементам контейнера 

что же должен уметь итератор? - разыменовывание,операции инкремента,возможность присваивания одного итератора другому(p=q//p и q - итераторы),сравнения итераторов через == и !=

теперь я перешел к итераторам,и к их видам,входные,выходные,однонаправленные,двунаправленные,произвольные - зачем?Ну каждый алгоритм нуждается в той или ином ввиде в этих видах итераторах.Например find нуждается в входном,а sort в произвольном.

входные итераторы - нужны для того,что бы считывать данные из контейнера,но не изменять их и входной итератор должен быть однонаправленным // чтение данных из контейнера

выходные итераторы - используются для однократного(однонаправленного) прохода по содержимому контейнера с доступом для записи // запись данных в контейнер?

однонаправленные итераторы - используются для прохода от начала до конца контейнера с помощью операции ++,также однонаправленные итераторы могут позволять только чтение либо чтение и запись 
зависит от 
int* pirw; // это итератор чтение и запись;

const int* pir; // итератор для чтения

сегодня еще изучали итераторы и кучу всего,что запомнил,это некоторые таблицы со свойствами итераторов и что имеется файл iterator в инклудах позволяющий делать такое:

std::ostream_iterator<int, char> out_iter(std::cout, " "); с помощью него можно выводить содержимое контейнеров если копировать например из вектора(по итераторам в начало и конец и копировать его в этот итератор,то мы как бы передаем это все в поток cout и выводим)

также рассматривали обратные итераторы rend и rbegin с помощью которых можно выводить элементы контейнеров обратным образом с конца например.Можно их объявлять явно а можно пользовать неявно:

явно:     std::vector<int>::reverse_iterator ri;
    for (ri = dice.rbegin(); ri != dice.rend(); ++ri)
    {
        std::cout << *ri << ' ';
    }

неявно: 
    std::copy(dice.rbegin(), dice.rend(), out_iter); // out_iter это тот самый итератор куда мы копируем содержимое контейнера с последнеего и выводим до начального только в обратном порядке
    std::cout << std::endl;

также можно заполнять контейнеры из массивов с помощью copy():
    int casts[10] = {6,7,2,9,4,11,8,7,10,5};
    std::vector<int> dice(10);

    std::copy(&casts[0], &casts[10], dice.begin());
    std::cout << "Let the dice be cast!\n";

и тут же вывод:
std::copy(dice.begin(), dice.end(), out_iter); // вывод снова с помощью итератор out_iter

тут в качестве итераторов выступают адресса первого и последнего элемента массива.

также сегодня я изучал другие итераторы которые могут увеличивать размер контейнера это back_insert_iterator<контейнер<его тип>>(название объекта контейнера) // этот итератор вставляет в конец контейнера данные и также увеличивает размер контейнера

insert_iterator<std::vector<std::string>>(words,words.begin()) // этот итератор вставляет в начало контейнера данные и также увеличивает размер контейнера

сами по себе они не могут увеличить размер или вставить данные в контейнер без функции copy(),но функция copy(),не увелечивает размер контейнеров,и это трюк через итераторы такой вот например:

std::copy(&s3[0], &s3[2], std::insert_iterator<std::vector<std::string>>(words,words.begin()) ); // это вставка в начало контейнера,данных из массива s3,с первого по последний элемент

std::copy(&s2[0], &s2[2], std::back_insert_iterator<std::vector<std::string>>(words) ); // эта вставка в конец контейнера данных из массива s2 с первого элемента до последнего,САМА ФУНКЦИЯ COPY() не увеличивают размеры контейнера,это делают итераторы

    std::vector<std::string> words(4);
    std::copy(&s1[0], &s1[4], words.begin()); // этот код например делает то,что функция copy() копирует весь массив с диапозона с 1 по последний элемент и вставляет в начало,как можем видеть она только может копировать и вставлять,но не увеличивать,но за счет итераторо back_insert_iterator и insert_iterator можно увеличивать ;-)

сегодня работал с контейнерами последовательные и ассоциативные
в чем фишка? последовательные типо vector и list имеют строго линейный порядок,есть первый а есть последний элеменет,и каждый элемент кроме первого и последнего имеет только один элемент перед ним и один элемент за ним.
ассоциативные работают по принципу ключей к данным(значениям) типо set и multipmap,они также позволяют вставлять элементы как последовательные контейнеры,но нельзя указать определенное место для вставки.
а еще изучил сложности контейнеров(от лучшей к худшей):
во время компиляции // все происходит на времени компиляции все просчеты,лучшая
постоянная // не зависит от кол-во элементов контейнера,средняя
линейная // худшая так как зависит от кол-во элементов контейнера.


virtual ~SomeClass()
родитель должен имееть виртуальный деструктор, то будет вызываться деструкторы наследника
Наследникам не обязательно

висячие указатели пример 
int* obj1 = new int{1}; // объявил указатели и выделил и инициализировал память
int* obj2 = new int{2}; // объявил указатели и выделил и инициализировал память
delete obj1; // очистил память у первого указателя в стеке
delete obj2; // очистил память у второго указателя в стеке
// ЗДЕСЬ указатели obj1 и obj2 - ВИСЯЧИЕ!
obj1 = nullptr;  // Теперь указатель безопасен и не являются висячими
obj2 = nullptr;  // Теперь указатель безопасен и не являются висячими

либо решать проблему висячих указателей с помощью умных указателей.
#include <memory>

// Вместо сырых указателей
std::unique_ptr<int> obj1 = std::make_unique<int>(1);
std::unique_ptr<int> obj2 = std::make_unique<int>(2);

// Не нужно вызывать delete - память освободится автоматически
// при выходе из области видимости

либо решать проблему висячих указателей Используя динамическое выделение памяти для локальных переменных в функциях, которые должны быть возвращены.:
int* getPointer()
{
    // Allocate on the heap
    int* localVar = new int(42);
    return localVar;
}

int main()
{

    // getting the pointer to the variable
    int* ptr = getPointer();
    cout << "Value: " << *ptr << endl;

    // manually deallocating it
    delete ptr;
    // setting ptr to null
    ptr = NULL;
    return 0;
}

либо использовать ссылки заместо указателей,для решения проблемы висячих указателей:
#include <iostream>
using namespace std;

int& getReference()
{
    // Use static to extend the lifetime
    static int localVar = 42;
    return localVar;
}

int main()
{
    int& ref = getReference();
    cout << "Value: " << ref << endl;
    return 0;
}

template<class T>
int reduce(T ar[], int n)
{
    std::vector<T> buffer;
    std::copy(ar, ar + n, std::back_insert_iterator<std::vector<T>>(buffer));
    std::sort(buffer.begin(), buffer.end());
    //std::vector<T>::iterator ip;
    auto ip = std::unique(buffer.begin(), buffer.end());
    buffer.resize(std::distance(buffer.begin(), ip));
    return buffer.size();
}

Таким образом, основное отличие заключается в том, что при префиксном(++i) инкременте значение увеличивается до использования, а при постфиксном(i++) — после.

возрат по ссылке использовать если:

Возрат параметра-ссылки

возврат элемента массива который был передан в функцию

при возврате большой структуры или класса,который не уничтожается в конце функции(например тот который был передан в функцию)

когда не использовать возраст по ссылке

при возврате переменных,которые были объявленны локально(внутри функции - возврат по значению)

при возврате стандартного массива или значения указателя(возврат по адрессу)

// UPD
/*
    для чего я делал placement new и разделял выделение памяти(для хранение объектов) и размещение объекта в памяти.
    потому что например при new = Someclass[100]; // вызывает конструкторы по умолчанию у класса Someclass.То есть простой оператор new выделяет память и вызывает конструкторы по умолчанию.100 все раз.
    если у Someclass нету конструкторов по умолчанию,вектор не будет компилироваться,при учете того что используется обычный new.
    если я выделяю память под 100 элементов но кладу один я все ровно заплатил бы(если бы был простой new) за создание 100 пустых объектов.А потом когда я делаю push_back,я бы их перезаписовал,двойна работа была бы проще говоря.
    также если мы используем ::operator new(кол-во например объектов * sizeof(тип объекта)) (ТАКЖЕ НУЖНО приводить эту память сырую к тому типу который нужен,ведь new возвращает void* поэтому нужно приводить кастами к типу) то и удалять надо через ::operator delete(указатель);
    перемещение объекта так вообще пришлось делать также через placement new и перемещение.И плюсом еще в цикле вызывать в старом блоке памяти у каждого отдельного объекта свой деструктор вручную.
    for (int i = 0; i < size; i++)
    {
        data[i].~T(); // уничтожаем каждый объект
    }
    ::operator delete(data); // освобождаем память

    это нужно для более сложных типов.
    вызывая для каждого дестркутор отдельно.А после удаляя сам массив.
    плюсом обычный delete[] указатель; // 1 - смотрит сколько элементов было в массив.После вызывает деструктор для всех этих элементов.Это бы сработало хорошо если бы мы хранили только простые типы.Но есть же и сложные такие как строки и тд.У них(строк и прочих классов) уже свои ресурсы.Также смешивался бы деструктор(который удалял бы через delete[] указатель; А конструктор или выделение памяти было бы через ::operator new глобальный) 
*/

int *a, *b, *c; // все указатели
int *d, e; // d - указатель, а e - это обычный int


Лямбда функции 

[]() // обычно они выглядят вот так.Анонимные функции.У них нет имен.В квадратные скобки передаются объекты по адресу из внешних областей видимости. 
{

};

также можно передавать функции как параметр в другие функции

    Dowork(vcc, [](int a) {std::cout << "Call anonim functional with parameters - " << a << '\n'; }); // тут в функцию передаем вектор и функцию,функция перебирает элементы,и элементы передает функции лямбде(std::function<void(int)> func) func(e1) вызывается в цикле к примеру

Также если лямбда функция ничего не возращает значит тип void иначе если присутствует в лямбда функции return хоть один,то значит причисляется какой-то тип.

В C++ 14 например можно стало быть вот так делать.

auto f = []()
{
	//тут что-то
};

f(); // тут функция вызывается.То есть мы по сути сделали имя функции.

Делались лямбда функции,что бы не делать,определение одной функции которая вызывается всего один раз и не захломлять код.


для закрепления прочитать можно еще про разделью компиляцию прочитать или посмотреть что-то.Остановился я на 911(по курсу книги - очень давняя запись)